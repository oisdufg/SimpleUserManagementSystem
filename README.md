# SimpleUserManagementSystem
A .NET backend application built with gRPC, Entity Framework Core, and MySQL. The solution follows a microservice-inspired architecture with separate services, API Gateway, repository pattern, soft delete, optimistic concurrency control, fuzzy search, and planned unit/integration testing.
